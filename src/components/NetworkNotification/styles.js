import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    padding: 20
  },
  header: {
    textAlign: 'center',
    marginBottom: 15
  },
  text: {
    marginBottom: 15
  }
});

export default styles;
